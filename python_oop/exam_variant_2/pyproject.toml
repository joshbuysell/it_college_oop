[project]
name = "exam-variant-2"
version = "0.1.0"
description = "Варіант 2 — Агент розрахунку геометричних фігур (ООП + Google ADK)"
authors = [
    {name = "Viktor Tsvyk"}
]
readme = "README.md"
requires-python = ">=3.11,<4.0.0"
dependencies = [
    "google-adk>=1.0.0",
    "python-dotenv>=1.0.0"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
